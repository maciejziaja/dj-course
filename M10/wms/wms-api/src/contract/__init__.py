"""Generated bindings for `openapi.yaml`.

`models.py` is written by `task contract-models` (scripts/generate_models.py) and
is not edited by hand - `task contract-lint` fails when it stops matching the
contract. Import from the module rather than the package so it stays obvious
where a name came from:

    from contract.models import Warehouse
"""
